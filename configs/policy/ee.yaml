schema_version: 1

# Minimal Cartesian policy profile. Unspecified numeric fields use the audited
# defaults documented in configs/policy/embodisteer.yaml.
policy:
  inference_space: ee
  num_inference_steps: 16
  guidance:
    method: ""
  baseline:
    method: ""
