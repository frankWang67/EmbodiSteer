schema_version: 1

# TurnOnSinkFaucet batch-sampling baseline used in the reference evaluation.
policy:
  inference_space: ee
  num_inference_steps: 16
  guidance:
    method: ""
  baseline:
    method: batch_sampling
    batch_sampling_num: 16
