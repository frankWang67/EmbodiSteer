schema_version: 1

# Sample Cartesian trajectories, solve IK, and select the safest candidate.
policy:
  inference_space: ee
  num_inference_steps: 16
  guidance:
    method: ""
  baseline:
    method: batch_sampling
    batch_sampling_num: 16
